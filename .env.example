# Required — your Google AI Studio / Vertex AI Gemini API key
GEMINI_API_KEY=your_gemini_api_key_here

# Optional — if set, clients must send this as x-api-key or Bearer token.
# Leave blank to accept any request (fine when the tunnel itself is your gate).
PROXY_API_KEY=

# Optional — defaults to 4747
PORT=4747
